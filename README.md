# A-Port-Scanner-Tool
A fast, multi threaded TCP port scanner written in python. Scans a target host over a given ports are open, identifies common services, and can optionallly grab service banners.
